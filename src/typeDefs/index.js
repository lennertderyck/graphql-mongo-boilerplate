const inputs = require('./inputs'),
    mutations = require('./mutations'),
    queries = require('./queries'),
    types = require('./types'),
    subcriptions = require('./subscriptions');

module.exports = [inputs, mutations, queries, types, subcriptions];
